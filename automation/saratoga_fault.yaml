id: saratoga_fault
alias: Saratoga Fault Report
trigger:
  - platform: mqtt
    topic: "saratoga/fault"
action:
  - action: notify.persistent_notification
    data:
      message: "{{ trigger.payload }}"
