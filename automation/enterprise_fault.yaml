id: enterprise_fault
alias: Enterprise Fault Report
trigger:
  - platform: mqtt
    topic: "enterprise/fault"
action:
  - action: notify.persistent_notification
    data:
      message: "{{ trigger.payload }}"
