id: saratoga_statechange
alias: Saratoga State Change
trigger:
  - platform: mqtt
    topic: "saratoga/status"
action:
  - service: notify.persistent_notification
    data:
      message: '{{ trigger.payload }}'
