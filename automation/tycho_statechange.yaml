id: tycho_statechange
alias: Tycho State Change
trigger:
  - platform: mqtt
    topic: "tycho/status"
action:
  - action: notify.persistent_notification
    data:
      message: "{{ trigger.payload }}"
